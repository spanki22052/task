@import '../../../common.styl';

* {
  font-family: 'Inter';
  font-style: normal;
}

.screenWrapper {
  width: 100%;

  h1 {
    margin-left: 28px;
    font-weight: 500;
    font-size: 28px;
    line-height: 34px;
    color: #1A2A83;
  }
}

.screen {
  coolScrollbar();
}

.items {
  display: block;
}
